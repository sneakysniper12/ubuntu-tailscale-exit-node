# Ubuntu Tailscale Exit Node + SSH

This project turns an Ubuntu system into:
- Exit Node
- Tailscale SSH host

## Features
- Exit node support
- Tailscale SSH (no port forwarding needed)
- Tag support

## Install

```bash
git clone https://github.com/YOUR_USERNAME/ubuntu-tailscale-exit-node.git
cd ubuntu-tailscale-exit-node
chmod +x install.sh
./install.sh
